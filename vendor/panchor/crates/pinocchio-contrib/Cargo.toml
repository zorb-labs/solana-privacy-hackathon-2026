[package]
name = "pinocchio-contrib"
version = "0.1.0"
edition = "2024"
description = "Utilities and extensions for writing better Pinocchio programs"
license = "MIT OR Apache-2.0"

[dependencies]
pinocchio = { workspace = true }
pinocchio-log = { workspace = true }
pinocchio-pubkey = { workspace = true }
pinocchio-system = { workspace = true }

[dev-dependencies]
pinocchio-test-utils = { path = "../pinocchio-test-utils" }
regex-lite = "0.1"

[features]
default = []

[lints]
workspace = true
